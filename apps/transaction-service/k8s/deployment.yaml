apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: analytics-service
  template:
    metadata:
      labels:
        app: analytics-service
    spec:
      containers:
        - name: analytics-service
          image: 311141556953.dkr.ecr.ap-southeast-5.amazonaws.com/asb-microservice/analytics-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: '50m'
              memory: '64Mi'
            limits:
              cpu: '250m'
              memory: '256Mi'
          envFrom:
            - secretRef:
                name: asb-secrets
