apiVersion: v1
kind: Service
metadata:
  name: diffusion-service
  namespace: staging
spec:
  type: NodePort
  selector:
    app: diffusion-service
  ports:
    - port: 80 # Service port
      targetPort: 3000 # Container port
      nodePort: 32088 # Unique NodePort (range: 30000–32767)
