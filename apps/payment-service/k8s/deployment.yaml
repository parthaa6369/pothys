apiVersion: apps/v1
kind: Deployment
metadata:
  name: document-service
  namespace: staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: document-service
  template:
    metadata:
      labels:
        app: document-service
    spec:
      containers:
        - name: document-service
          image: 311141556953.dkr.ecr.ap-southeast-5.amazonaws.com/asb-microservice/document-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: '50m'
              memory: '64Mi'
            limits:
              cpu: '250m'
              memory: '256Mi'
          envFrom:
            - secretRef:
                name: asb-secrets
