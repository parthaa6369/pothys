apiVersion: v1
kind: Service
metadata:
  name: document-service
  namespace: staging
spec:
  type: NodePort
  selector:
    app: document-service
  ports:
    - port: 80          # Service port
      targetPort: 3000  # Container port
      nodePort: 32083   # Unique NodePort (range: 30000–32767)
